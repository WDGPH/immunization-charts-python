"""Placeholder coverage for compile_notices.

The parallel implementation was deferred to a future PR, so these behavioural
tests are intentionally skipped for now. Once Task 4 lands, replace thismodule
with focused coverage that matches the updated contract.
"""

import pytest


pytest.skip("compile_notices parallel tests deferred", allow_module_level=True)
