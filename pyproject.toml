[project]
name = "immunization-charts-python"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "PyYAML",
    "openpyxl",
    "PyPDF2",
    "typst>=0.13.2",
]

[dependency-groups]
dev = [
    "ipykernel",
]
