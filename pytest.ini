# pytest.ini
[pytest]
pythonpath = scripts

testpaths = tests

markers =
    unit: Unit tests for individual modules (fast, <100ms)
    integration: Integration tests for step interactions (medium, 100ms-1s)
    e2e: End-to-end pipeline tests (slow, 1s-30s)
